# Constrained manifest logic to gameplay composition

Archetype manifests use structured conditions and effect sequences with small arithmetic expressions. We excluded embedded scripting and unrestricted loops to keep manifests readable and their behavior inspectable, accepting Kotlin extensions for new interactions with Minecraft. Reusable ability definitions use explicit references and parameters without inheritance chains.
