# Required client installation with server-controlled gameplay

Archetype requires installation on player clients and the server so abilities can use custom controls, targeting feedback, and resource displays. The server controls gameplay outcomes, while the client supplies input and presentation. We accepted the installation requirement instead of constraining these features to support unmodified clients.
